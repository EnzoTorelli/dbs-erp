From 697fbc355aab587d5596029db8b20912856bf362 Mon Sep 17 00:00:00 2001
From: Claude <claude@anthropic.com>
Date: Mon, 18 May 2026 18:51:48 +0000
Subject: [PATCH] feat: conecta backend ao frontend

- Cria Models: Cliente, Produto, Pedido, Categoria
- Cria ViewModels: DashboardViewModel
- Cria Repositories: ClienteRepository, ProdutoRepository, PedidoRepository
- Atualiza todos os Controllers com dados reais do banco
- Implementa login com autenticacao por sessao (Session)
- Atualiza Views para usar @model real (sem dados mockados)
- Adiciona filtro de busca por texto nas tabelas
- Atualiza Dashboard com metricas reais do banco
- Configura appsettings.json com ConnectionString MySQL
- Atualiza Program.cs com Session e injecao de dependencia
- Adiciona pacote MySql.Data 8.3.0 no .csproj
---
 DBS/DBS/Controllers/ClientesController.cs  |  26 +++-
 DBS/DBS/Controllers/DashboardController.cs |  35 ++++-
 DBS/DBS/Controllers/LoginController.cs     |  18 ++-
 DBS/DBS/Controllers/OrdensController.cs    |  37 +++---
 DBS/DBS/Controllers/ProdutosController.cs  |  26 +++-
 DBS/DBS/Controllers/VendasController.cs    |  38 +++---
 DBS/DBS/DBS.csproj                         |   4 +
 DBS/DBS/Models/Categoria.cs                |   8 ++
 DBS/DBS/Models/Cliente.cs                  |  12 ++
 DBS/DBS/Models/Pedido.cs                   |  12 ++
 DBS/DBS/Models/Produto.cs                  |  21 +++
 DBS/DBS/Program.cs                         |  23 +++-
 DBS/DBS/Repositories/ClienteRepository.cs  |  72 +++++++++++
 DBS/DBS/Repositories/PedidoRepository.cs   | 122 +++++++++++++++++
 DBS/DBS/Repositories/ProdutoRepository.cs  |  76 +++++++++++
 DBS/DBS/ViewModels/DashboardViewModel.cs   |  13 ++
 DBS/DBS/Views/Clientes/Index.cshtml        | 130 ++++++-------------
 DBS/DBS/Views/Dashboard/Index.cshtml       | 117 ++++++++---------
 DBS/DBS/Views/Ordens/Index.cshtml          |  47 ++++---
 DBS/DBS/Views/Produtos/Index.cshtml        | 144 ++++++---------------
 DBS/DBS/Views/Vendas/Index.cshtml          |  43 +++---
 DBS/DBS/appsettings.json                   |   5 +-
 22 files changed, 692 insertions(+), 337 deletions(-)
 create mode 100644 DBS/DBS/Models/Categoria.cs
 create mode 100644 DBS/DBS/Models/Cliente.cs
 create mode 100644 DBS/DBS/Models/Pedido.cs
 create mode 100644 DBS/DBS/Models/Produto.cs
 create mode 100644 DBS/DBS/Repositories/ClienteRepository.cs
 create mode 100644 DBS/DBS/Repositories/PedidoRepository.cs
 create mode 100644 DBS/DBS/Repositories/ProdutoRepository.cs
 create mode 100644 DBS/DBS/ViewModels/DashboardViewModel.cs

diff --git a/DBS/DBS/Controllers/ClientesController.cs b/DBS/DBS/Controllers/ClientesController.cs
index ed5674d..8b81056 100644
--- a/DBS/DBS/Controllers/ClientesController.cs
+++ b/DBS/DBS/Controllers/ClientesController.cs
@@ -1,14 +1,34 @@
-﻿using Microsoft.AspNetCore.Mvc;
+using DBS.Repositories;
+using Microsoft.AspNetCore.Mvc;
 
 namespace DBS.Controllers
 {
     public class ClientesController : Controller
     {
+        private readonly ClienteRepository _repo;
+
+        public ClientesController(ClienteRepository repo)
+        {
+            _repo = repo;
+        }
+
         public IActionResult Index()
         {
-            ViewData["Title"] = "Cliente";
+            if (HttpContext.Session.GetString("Usuario") == null)
+                return RedirectToAction("Index", "Login");
+
+            ViewData["Title"] = "Clientes";
             ViewData["Pagina"] = "Clientes";
-            return View();
+
+            var clientes = _repo.GetAll();
+            return View(clientes);
+        }
+
+        [HttpPost]
+        public IActionResult Excluir(int id)
+        {
+            _repo.Delete(id);
+            return RedirectToAction("Index");
         }
     }
 }
diff --git a/DBS/DBS/Controllers/DashboardController.cs b/DBS/DBS/Controllers/DashboardController.cs
index a6e2d4d..262b2ce 100644
--- a/DBS/DBS/Controllers/DashboardController.cs
+++ b/DBS/DBS/Controllers/DashboardController.cs
@@ -1,14 +1,43 @@
-﻿using Microsoft.AspNetCore.Mvc;
+using DBS.Repositories;
+using DBS.ViewModels;
+using Microsoft.AspNetCore.Mvc;
 
 namespace DBS.Controllers
 {
     public class DashboardController : Controller
     {
+        private readonly ClienteRepository _clienteRepo;
+        private readonly ProdutoRepository _produtoRepo;
+        private readonly PedidoRepository  _pedidoRepo;
+
+        public DashboardController(
+            ClienteRepository clienteRepo,
+            ProdutoRepository produtoRepo,
+            PedidoRepository  pedidoRepo)
+        {
+            _clienteRepo = clienteRepo;
+            _produtoRepo = produtoRepo;
+            _pedidoRepo  = pedidoRepo;
+        }
+
         public IActionResult Index()
         {
+            if (HttpContext.Session.GetString("Usuario") == null)
+                return RedirectToAction("Index", "Login");
+
             ViewData["Title"] = "Dashboard";
             ViewData["Pagina"] = "Dashboard";
-            return View();
+
+            var vm = new DashboardViewModel
+            {
+                ReceitaMes           = _pedidoRepo.ReceitaMes(),
+                TotalClientesAtivos  = _clienteRepo.Count(),
+                TotalOrdensAbertas   = _pedidoRepo.CountAbertos(),
+                TotalProdutosEstoque = _produtoRepo.TotalEstoque(),
+                UltimasOrdens        = _pedidoRepo.GetUltimas(5)
+            };
+
+            return View(vm);
         }
     }
-}
\ No newline at end of file
+}
diff --git a/DBS/DBS/Controllers/LoginController.cs b/DBS/DBS/Controllers/LoginController.cs
index dad2255..38bc607 100644
--- a/DBS/DBS/Controllers/LoginController.cs
+++ b/DBS/DBS/Controllers/LoginController.cs
@@ -1,25 +1,35 @@
-﻿using Microsoft.AspNetCore.Mvc;
+using Microsoft.AspNetCore.Mvc;
 
 namespace DBS.Controllers
 {
     public class LoginController : Controller
     {
-        // Abre a tela de login
         public IActionResult Index()
         {
+            // Se já estiver logado, vai direto pro dashboard
+            if (HttpContext.Session.GetString("Usuario") != null)
+                return RedirectToAction("Index", "Dashboard");
+
             return View();
         }
 
-        // Recebe os dados do formulário
         [HttpPost]
         public IActionResult Index(string email, string senha)
         {
             if (email == "admin" && senha == "123")
             {
+                HttpContext.Session.SetString("Usuario", email);
                 return RedirectToAction("Index", "Dashboard");
             }
 
+            ViewBag.Erro = "Usuário ou senha inválidos.";
             return View();
         }
+
+        public IActionResult Logout()
+        {
+            HttpContext.Session.Clear();
+            return RedirectToAction("Index", "Login");
+        }
     }
-}
\ No newline at end of file
+}
diff --git a/DBS/DBS/Controllers/OrdensController.cs b/DBS/DBS/Controllers/OrdensController.cs
index c7c38ce..3598832 100644
--- a/DBS/DBS/Controllers/OrdensController.cs
+++ b/DBS/DBS/Controllers/OrdensController.cs
@@ -1,27 +1,34 @@
-﻿using Microsoft.AspNetCore.Mvc;
-using System.Collections.Generic;
+using DBS.Repositories;
+using Microsoft.AspNetCore.Mvc;
 
 namespace DBS.Controllers
 {
     public class OrdensController : Controller
     {
+        private readonly PedidoRepository _repo;
+
+        public OrdensController(PedidoRepository repo)
+        {
+            _repo = repo;
+        }
+
         public IActionResult Index()
         {
-            var ordens = new List<Ordem>
-            {
-                new Ordem { Id = 4521, ClienteNome = "Ana Costa", Valor = 890, Status = "Pago" },
-                new Ordem { Id = 4520, ClienteNome = "Carlos Souza", Valor = 120, Status = "Pendente" }
-            };
+            if (HttpContext.Session.GetString("Usuario") == null)
+                return RedirectToAction("Index", "Login");
+
+            ViewData["Title"] = "Ordens";
+            ViewData["Pagina"] = "Ordens";
 
+            var ordens = _repo.GetAll();
             return View(ordens);
         }
-    }
 
-    public class Ordem
-    {
-        public int Id { get; set; }
-        public string ClienteNome { get; set; }
-        public decimal Valor { get; set; }
-        public string Status { get; set; }
+        [HttpPost]
+        public IActionResult Excluir(int id)
+        {
+            _repo.Delete(id);
+            return RedirectToAction("Index");
+        }
     }
-}
\ No newline at end of file
+}
diff --git a/DBS/DBS/Controllers/ProdutosController.cs b/DBS/DBS/Controllers/ProdutosController.cs
index fac1dde..c23ac9a 100644
--- a/DBS/DBS/Controllers/ProdutosController.cs
+++ b/DBS/DBS/Controllers/ProdutosController.cs
@@ -1,14 +1,34 @@
-﻿using Microsoft.AspNetCore.Mvc;
+using DBS.Repositories;
+using Microsoft.AspNetCore.Mvc;
 
 namespace DBS.Controllers
 {
     public class ProdutosController : Controller
     {
+        private readonly ProdutoRepository _repo;
+
+        public ProdutosController(ProdutoRepository repo)
+        {
+            _repo = repo;
+        }
+
         public IActionResult Index()
         {
+            if (HttpContext.Session.GetString("Usuario") == null)
+                return RedirectToAction("Index", "Login");
+
             ViewData["Title"] = "Produtos";
             ViewData["Pagina"] = "Produtos";
-            return View();
+
+            var produtos = _repo.GetAll();
+            return View(produtos);
+        }
+
+        [HttpPost]
+        public IActionResult Excluir(int id)
+        {
+            _repo.Delete(id);
+            return RedirectToAction("Index");
         }
     }
-}
\ No newline at end of file
+}
diff --git a/DBS/DBS/Controllers/VendasController.cs b/DBS/DBS/Controllers/VendasController.cs
index 66df872..c860682 100644
--- a/DBS/DBS/Controllers/VendasController.cs
+++ b/DBS/DBS/Controllers/VendasController.cs
@@ -1,29 +1,29 @@
-﻿using Microsoft.AspNetCore.Mvc;
-using System;
-using System.Collections.Generic;
+using DBS.Repositories;
+using Microsoft.AspNetCore.Mvc;
 
 namespace DBS.Controllers
 {
+    // Vendas = pedidos com status "Pago"
     public class VendasController : Controller
     {
+        private readonly PedidoRepository _repo;
+
+        public VendasController(PedidoRepository repo)
+        {
+            _repo = repo;
+        }
+
         public IActionResult Index()
         {
-            var vendas = new List<Venda>
-            {
-                new Venda { Id = 1001, ClienteNome = "Maria Silva", Valor = 320, Data = DateTime.Now, Status = "Pago" },
-                new Venda { Id = 1002, ClienteNome = "João Pereira", Valor = 150, Data = DateTime.Now, Status = "Pendente" }
-            };
+            if (HttpContext.Session.GetString("Usuario") == null)
+                return RedirectToAction("Index", "Login");
 
-            return View(vendas);
-        }
-    }
+            ViewData["Title"] = "Vendas";
+            ViewData["Pagina"] = "Vendas";
 
-    public class Venda
-    {
-        public int Id { get; set; }
-        public string ClienteNome { get; set; }
-        public decimal Valor { get; set; }
-        public DateTime Data { get; set; }
-        public string Status { get; set; }
+            // Reutiliza GetAll — a view pode filtrar, ou podemos trazer todos os pedidos
+            var pedidos = _repo.GetAll();
+            return View(pedidos);
+        }
     }
-}
\ No newline at end of file
+}
diff --git a/DBS/DBS/DBS.csproj b/DBS/DBS/DBS.csproj
index 6b7675a..b89e523 100644
--- a/DBS/DBS/DBS.csproj
+++ b/DBS/DBS/DBS.csproj
@@ -6,6 +6,10 @@
     <ImplicitUsings>enable</ImplicitUsings>
   </PropertyGroup>
 
+  <ItemGroup>
+    <PackageReference Include="MySql.Data" Version="8.3.0" />
+  </ItemGroup>
+
   <ItemGroup>
     <Compile Remove="wwwroot\css\NewFolder\**" />
     <Compile Remove="wwwroot\NewFolder1\**" />
diff --git a/DBS/DBS/Models/Categoria.cs b/DBS/DBS/Models/Categoria.cs
new file mode 100644
index 0000000..48db3de
--- /dev/null
+++ b/DBS/DBS/Models/Categoria.cs
@@ -0,0 +1,8 @@
+namespace DBS.Models
+{
+    public class Categoria
+    {
+        public int Id { get; set; }
+        public string Nome { get; set; } = string.Empty;
+    }
+}
diff --git a/DBS/DBS/Models/Cliente.cs b/DBS/DBS/Models/Cliente.cs
new file mode 100644
index 0000000..d74e197
--- /dev/null
+++ b/DBS/DBS/Models/Cliente.cs
@@ -0,0 +1,12 @@
+namespace DBS.Models
+{
+    public class Cliente
+    {
+        public int Id { get; set; }
+        public string Nome { get; set; } = string.Empty;
+        public string? Cpf { get; set; }
+        public string? Email { get; set; }
+        public string? Telefone { get; set; }
+        public DateTime DataCadastro { get; set; }
+    }
+}
diff --git a/DBS/DBS/Models/Pedido.cs b/DBS/DBS/Models/Pedido.cs
new file mode 100644
index 0000000..838d6e6
--- /dev/null
+++ b/DBS/DBS/Models/Pedido.cs
@@ -0,0 +1,12 @@
+namespace DBS.Models
+{
+    public class Pedido
+    {
+        public int Id { get; set; }
+        public int? IdCliente { get; set; }
+        public string? ClienteNome { get; set; }
+        public DateTime DataPedido { get; set; }
+        public string? Status { get; set; }
+        public decimal ValorTotal { get; set; }
+    }
+}
diff --git a/DBS/DBS/Models/Produto.cs b/DBS/DBS/Models/Produto.cs
new file mode 100644
index 0000000..02e0c14
--- /dev/null
+++ b/DBS/DBS/Models/Produto.cs
@@ -0,0 +1,21 @@
+namespace DBS.Models
+{
+    public class Produto
+    {
+        public int Id { get; set; }
+        public string Nome { get; set; } = string.Empty;
+        public string? Descricao { get; set; }
+        public decimal Preco { get; set; }
+        public int Estoque { get; set; }
+        public int? IdCategoria { get; set; }
+        public string? CategoriaNome { get; set; }
+
+        public string StatusEstoque => Estoque == 0 ? "indisponivel"
+                                     : Estoque <= 5 ? "baixo-estoque"
+                                     : "disponivel";
+
+        public string StatusEstoqueLabel => Estoque == 0 ? "Indisponível"
+                                           : Estoque <= 5 ? "Baixo Estoque"
+                                           : "Disponível";
+    }
+}
diff --git a/DBS/DBS/Program.cs b/DBS/DBS/Program.cs
index fc04252..0368520 100644
--- a/DBS/DBS/Program.cs
+++ b/DBS/DBS/Program.cs
@@ -1,15 +1,29 @@
+using DBS.Repositories;
+
 var builder = WebApplication.CreateBuilder(args);
 
-// Add services to the container.
+// MVC
 builder.Services.AddControllersWithViews();
 
+// Sessão (para o login funcionar)
+builder.Services.AddDistributedMemoryCache();
+builder.Services.AddSession(options =>
+{
+    options.IdleTimeout = TimeSpan.FromMinutes(30);
+    options.Cookie.HttpOnly = true;
+    options.Cookie.IsEssential = true;
+});
+
+// Injeção de dependência dos repositórios
+builder.Services.AddScoped<ClienteRepository>();
+builder.Services.AddScoped<ProdutoRepository>();
+builder.Services.AddScoped<PedidoRepository>();
+
 var app = builder.Build();
 
-// Configure the HTTP request pipeline.
 if (!app.Environment.IsDevelopment())
 {
     app.UseExceptionHandler("/Home/Error");
-    // The default HSTS value is 30 days. You may want to change this for production scenarios, see https://aka.ms/aspnetcore-hsts.
     app.UseHsts();
 }
 
@@ -18,10 +32,11 @@ app.UseStaticFiles();
 
 app.UseRouting();
 
+app.UseSession(); // deve vir antes do UseAuthorization
 app.UseAuthorization();
 
 app.MapControllerRoute(
     name: "default",
-    pattern: "{controller=login}/{action=Index}/{id?}");
+    pattern: "{controller=Login}/{action=Index}/{id?}");
 
 app.Run();
diff --git a/DBS/DBS/Repositories/ClienteRepository.cs b/DBS/DBS/Repositories/ClienteRepository.cs
new file mode 100644
index 0000000..610d055
--- /dev/null
+++ b/DBS/DBS/Repositories/ClienteRepository.cs
@@ -0,0 +1,72 @@
+using DBS.Models;
+using MySql.Data.MySqlClient;
+
+namespace DBS.Repositories
+{
+    public class ClienteRepository
+    {
+        private readonly string _connectionString;
+
+        public ClienteRepository(IConfiguration configuration)
+        {
+            _connectionString = configuration.GetConnectionString("DefaultConnection")!;
+        }
+
+        public List<Cliente> GetAll()
+        {
+            var clientes = new List<Cliente>();
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+
+            var cmd = new MySqlCommand(
+                "SELECT id, nome, cpf, email, telefone, data_cadastro FROM cliente ORDER BY nome",
+                conn);
+
+            using var reader = cmd.ExecuteReader();
+            while (reader.Read())
+            {
+                clientes.Add(new Cliente
+                {
+                    Id           = reader.GetInt32("id"),
+                    Nome         = reader.GetString("nome"),
+                    Cpf          = reader.IsDBNull(reader.GetOrdinal("cpf"))      ? null : reader.GetString("cpf"),
+                    Email        = reader.IsDBNull(reader.GetOrdinal("email"))     ? null : reader.GetString("email"),
+                    Telefone     = reader.IsDBNull(reader.GetOrdinal("telefone"))  ? null : reader.GetString("telefone"),
+                    DataCadastro = reader.GetDateTime("data_cadastro")
+                });
+            }
+            return clientes;
+        }
+
+        public int Count()
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand("SELECT COUNT(*) FROM cliente", conn);
+            return Convert.ToInt32(cmd.ExecuteScalar());
+        }
+
+        public void Insert(Cliente cliente)
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand(
+                "INSERT INTO cliente (nome, cpf, email, telefone) VALUES (@nome, @cpf, @email, @telefone)",
+                conn);
+            cmd.Parameters.AddWithValue("@nome",     cliente.Nome);
+            cmd.Parameters.AddWithValue("@cpf",      cliente.Cpf);
+            cmd.Parameters.AddWithValue("@email",    cliente.Email);
+            cmd.Parameters.AddWithValue("@telefone", cliente.Telefone);
+            cmd.ExecuteNonQuery();
+        }
+
+        public void Delete(int id)
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand("DELETE FROM cliente WHERE id = @id", conn);
+            cmd.Parameters.AddWithValue("@id", id);
+            cmd.ExecuteNonQuery();
+        }
+    }
+}
diff --git a/DBS/DBS/Repositories/PedidoRepository.cs b/DBS/DBS/Repositories/PedidoRepository.cs
new file mode 100644
index 0000000..1f8a962
--- /dev/null
+++ b/DBS/DBS/Repositories/PedidoRepository.cs
@@ -0,0 +1,122 @@
+using DBS.Models;
+using MySql.Data.MySqlClient;
+
+namespace DBS.Repositories
+{
+    public class PedidoRepository
+    {
+        private readonly string _connectionString;
+
+        public PedidoRepository(IConfiguration configuration)
+        {
+            _connectionString = configuration.GetConnectionString("DefaultConnection")!;
+        }
+
+        public List<Pedido> GetAll()
+        {
+            var pedidos = new List<Pedido>();
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+
+            var cmd = new MySqlCommand(@"
+                SELECT p.id, p.id_cliente, c.nome AS cliente_nome, p.data_pedido, p.status,
+                       COALESCE(SUM(ip.quantidade * ip.preco_unitario), 0) AS valor_total
+                FROM pedido p
+                LEFT JOIN cliente c ON c.id = p.id_cliente
+                LEFT JOIN item_pedido ip ON ip.id_pedido = p.id
+                GROUP BY p.id, p.id_cliente, c.nome, p.data_pedido, p.status
+                ORDER BY p.data_pedido DESC", conn);
+
+            using var reader = cmd.ExecuteReader();
+            while (reader.Read())
+            {
+                pedidos.Add(MapPedido(reader));
+            }
+            return pedidos;
+        }
+
+        public List<Pedido> GetUltimas(int quantidade = 5)
+        {
+            var pedidos = new List<Pedido>();
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+
+            var cmd = new MySqlCommand(@"
+                SELECT p.id, p.id_cliente, c.nome AS cliente_nome, p.data_pedido, p.status,
+                       COALESCE(SUM(ip.quantidade * ip.preco_unitario), 0) AS valor_total
+                FROM pedido p
+                LEFT JOIN cliente c ON c.id = p.id_cliente
+                LEFT JOIN item_pedido ip ON ip.id_pedido = p.id
+                GROUP BY p.id, p.id_cliente, c.nome, p.data_pedido, p.status
+                ORDER BY p.data_pedido DESC
+                LIMIT @qtd", conn);
+            cmd.Parameters.AddWithValue("@qtd", quantidade);
+
+            using var reader = cmd.ExecuteReader();
+            while (reader.Read())
+            {
+                pedidos.Add(MapPedido(reader));
+            }
+            return pedidos;
+        }
+
+        public int CountAbertos()
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand(
+                "SELECT COUNT(*) FROM pedido WHERE status = 'Pendente' OR status IS NULL", conn);
+            return Convert.ToInt32(cmd.ExecuteScalar());
+        }
+
+        public decimal ReceitaMes()
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand(@"
+                SELECT COALESCE(SUM(ip.quantidade * ip.preco_unitario), 0)
+                FROM pedido p
+                JOIN item_pedido ip ON ip.id_pedido = p.id
+                WHERE MONTH(p.data_pedido) = MONTH(CURDATE())
+                  AND YEAR(p.data_pedido)  = YEAR(CURDATE())
+                  AND p.status = 'Pago'", conn);
+            return Convert.ToDecimal(cmd.ExecuteScalar());
+        }
+
+        public void Insert(Pedido pedido)
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand(
+                "INSERT INTO pedido (id_cliente, status) VALUES (@idCliente, @status)",
+                conn);
+            cmd.Parameters.AddWithValue("@idCliente", pedido.IdCliente);
+            cmd.Parameters.AddWithValue("@status",    pedido.Status ?? "Pendente");
+            cmd.ExecuteNonQuery();
+        }
+
+        public void Delete(int id)
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            // Remove itens primeiro (FK)
+            var cmdItens = new MySqlCommand("DELETE FROM item_pedido WHERE id_pedido = @id", conn);
+            cmdItens.Parameters.AddWithValue("@id", id);
+            cmdItens.ExecuteNonQuery();
+
+            var cmd = new MySqlCommand("DELETE FROM pedido WHERE id = @id", conn);
+            cmd.Parameters.AddWithValue("@id", id);
+            cmd.ExecuteNonQuery();
+        }
+
+        private static Pedido MapPedido(MySqlDataReader reader) => new Pedido
+        {
+            Id          = reader.GetInt32("id"),
+            IdCliente   = reader.IsDBNull(reader.GetOrdinal("id_cliente"))    ? null : reader.GetInt32("id_cliente"),
+            ClienteNome = reader.IsDBNull(reader.GetOrdinal("cliente_nome"))  ? "—"  : reader.GetString("cliente_nome"),
+            DataPedido  = reader.IsDBNull(reader.GetOrdinal("data_pedido"))   ? DateTime.MinValue : reader.GetDateTime("data_pedido"),
+            Status      = reader.IsDBNull(reader.GetOrdinal("status"))        ? "Pendente" : reader.GetString("status"),
+            ValorTotal  = reader.GetDecimal("valor_total")
+        };
+    }
+}
diff --git a/DBS/DBS/Repositories/ProdutoRepository.cs b/DBS/DBS/Repositories/ProdutoRepository.cs
new file mode 100644
index 0000000..1af9882
--- /dev/null
+++ b/DBS/DBS/Repositories/ProdutoRepository.cs
@@ -0,0 +1,76 @@
+using DBS.Models;
+using MySql.Data.MySqlClient;
+
+namespace DBS.Repositories
+{
+    public class ProdutoRepository
+    {
+        private readonly string _connectionString;
+
+        public ProdutoRepository(IConfiguration configuration)
+        {
+            _connectionString = configuration.GetConnectionString("DefaultConnection")!;
+        }
+
+        public List<Produto> GetAll()
+        {
+            var produtos = new List<Produto>();
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+
+            var cmd = new MySqlCommand(@"
+                SELECT p.id, p.nome, p.descricao, p.preco, p.estoque, p.id_categoria, c.nome AS categoria_nome
+                FROM produto p
+                LEFT JOIN categoria c ON c.id = p.id_categoria
+                ORDER BY p.nome", conn);
+
+            using var reader = cmd.ExecuteReader();
+            while (reader.Read())
+            {
+                produtos.Add(new Produto
+                {
+                    Id           = reader.GetInt32("id"),
+                    Nome         = reader.GetString("nome"),
+                    Descricao    = reader.IsDBNull(reader.GetOrdinal("descricao"))       ? null : reader.GetString("descricao"),
+                    Preco        = reader.GetDecimal("preco"),
+                    Estoque      = reader.GetInt32("estoque"),
+                    IdCategoria  = reader.IsDBNull(reader.GetOrdinal("id_categoria"))    ? null : reader.GetInt32("id_categoria"),
+                    CategoriaNome = reader.IsDBNull(reader.GetOrdinal("categoria_nome")) ? null : reader.GetString("categoria_nome")
+                });
+            }
+            return produtos;
+        }
+
+        public int TotalEstoque()
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand("SELECT COALESCE(SUM(estoque), 0) FROM produto", conn);
+            return Convert.ToInt32(cmd.ExecuteScalar());
+        }
+
+        public void Insert(Produto produto)
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand(
+                "INSERT INTO produto (nome, descricao, preco, estoque, id_categoria) VALUES (@nome, @descricao, @preco, @estoque, @idCategoria)",
+                conn);
+            cmd.Parameters.AddWithValue("@nome",        produto.Nome);
+            cmd.Parameters.AddWithValue("@descricao",   produto.Descricao);
+            cmd.Parameters.AddWithValue("@preco",       produto.Preco);
+            cmd.Parameters.AddWithValue("@estoque",     produto.Estoque);
+            cmd.Parameters.AddWithValue("@idCategoria", produto.IdCategoria);
+            cmd.ExecuteNonQuery();
+        }
+
+        public void Delete(int id)
+        {
+            using var conn = new MySqlConnection(_connectionString);
+            conn.Open();
+            var cmd = new MySqlCommand("DELETE FROM produto WHERE id = @id", conn);
+            cmd.Parameters.AddWithValue("@id", id);
+            cmd.ExecuteNonQuery();
+        }
+    }
+}
diff --git a/DBS/DBS/ViewModels/DashboardViewModel.cs b/DBS/DBS/ViewModels/DashboardViewModel.cs
new file mode 100644
index 0000000..924d036
--- /dev/null
+++ b/DBS/DBS/ViewModels/DashboardViewModel.cs
@@ -0,0 +1,13 @@
+using DBS.Models;
+
+namespace DBS.ViewModels
+{
+    public class DashboardViewModel
+    {
+        public decimal ReceitaMes { get; set; }
+        public int TotalClientesAtivos { get; set; }
+        public int TotalOrdensAbertas { get; set; }
+        public int TotalProdutosEstoque { get; set; }
+        public List<Pedido> UltimasOrdens { get; set; } = new();
+    }
+}
diff --git a/DBS/DBS/Views/Clientes/Index.cshtml b/DBS/DBS/Views/Clientes/Index.cshtml
index 0254bc2..1ea2439 100644
--- a/DBS/DBS/Views/Clientes/Index.cshtml
+++ b/DBS/DBS/Views/Clientes/Index.cshtml
@@ -1,4 +1,5 @@
-﻿@{
+@model List<DBS.Models.Cliente>
+@{
     Layout = "~/Views/Shared/_Layout.cshtml";
     ViewData["Title"] = "Clientes";
     ViewData["Pagina"] = "Clientes";
@@ -20,88 +21,48 @@
     </div>
 
     <div class="barra-busca">
-        <input type="text" placeholder="Buscar por nome, e-mail ou CPF..." />
-        <select>
-            <option value="">Todos os status</option>
-            <option value="ativo">Ativo</option>
-            <option value="inativo">Inativo</option>
-        </select>
+        <input type="text" id="campoBusca" placeholder="Buscar por nome, e-mail ou CPF..." oninput="filtrarTabela()" />
     </div>
 
     <div class="painel">
-        <table class="tabela">
+        <table class="tabela" id="tabelaClientes">
             <thead>
                 <tr>
                     <th>#</th>
                     <th>Nome</th>
                     <th>E-mail</th>
                     <th>Telefone</th>
-                    <th>Cidade</th>
-                    <th>Status</th>
+                    <th>CPF</th>
+                    <th>Cadastro</th>
                     <th>Ações</th>
                 </tr>
             </thead>
             <tbody>
-                <tr>
-                    <td>001</td>
-                    <td>Maria Silva</td>
-                    <td>maria@email.com</td>
-                    <td>(11) 99999-1111</td>
-                    <td>São Paulo</td>
-                    <td><span class="status ativo">Ativo</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
-                <tr>
-                    <td>002</td>
-                    <td>João Pereira</td>
-                    <td>joao@email.com</td>
-                    <td>(11) 99999-2222</td>
-                    <td>Campinas</td>
-                    <td><span class="status ativo">Ativo</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
-                <tr>
-                    <td>003</td>
-                    <td>Ana Costa</td>
-                    <td>ana@email.com</td>
-                    <td>(11) 99999-3333</td>
-                    <td>Santos</td>
-                    <td><span class="status inativo">Inativo</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
-                <tr>
-                    <td>004</td>
-                    <td>Carlos Lima</td>
-                    <td>carlos@email.com</td>
-                    <td>(11) 99999-4444</td>
-                    <td>Sorocaba</td>
-                    <td><span class="status ativo">Ativo</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
-                <tr>
-                    <td>005</td>
-                    <td>Paula Souza</td>
-                    <td>paula@email.com</td>
-                    <td>(11) 99999-5555</td>
-                    <td>Jundiaí</td>
-                    <td><span class="status ativo">Ativo</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
+                @if (!Model.Any())
+                {
+                    <tr>
+                        <td colspan="7" style="text-align:center; color:#888;">Nenhum cliente cadastrado.</td>
+                    </tr>
+                }
+                @foreach (var cliente in Model)
+                {
+                    <tr>
+                        <td>@cliente.Id</td>
+                        <td>@cliente.Nome</td>
+                        <td>@(cliente.Email ?? "—")</td>
+                        <td>@(cliente.Telefone ?? "—")</td>
+                        <td>@(cliente.Cpf ?? "—")</td>
+                        <td>@cliente.DataCadastro.ToString("dd/MM/yyyy")</td>
+                        <td>
+                            <button class="btn-acao editar">Editar</button>
+                            <form method="post" asp-action="Excluir" asp-route-id="@cliente.Id" style="display:inline"
+                                  onsubmit="return confirm('Excluir cliente @cliente.Nome?')">
+                                @Html.AntiForgeryToken()
+                                <button type="submit" class="btn-acao excluir">Excluir</button>
+                            </form>
+                        </td>
+                    </tr>
+                }
             </tbody>
         </table>
     </div>
@@ -137,20 +98,6 @@
             <input type="email" placeholder="email@exemplo.com" />
         </div>
 
-        <div class="form-linha">
-            <div class="form-grupo">
-                <label>Cidade</label>
-                <input type="text" placeholder="Ex: São Paulo" />
-            </div>
-            <div class="form-grupo">
-                <label>Status</label>
-                <select>
-                    <option value="ativo">Ativo</option>
-                    <option value="inativo">Inativo</option>
-                </select>
-            </div>
-        </div>
-
         <div class="modal-footer">
             <button class="btn-cancelar" onclick="fecharModal()">Cancelar</button>
             <button class="btn-salvar">Salvar Cliente</button>
@@ -163,14 +110,19 @@
         function abrirModal() {
             document.getElementById('modalCliente').classList.add('aberto');
         }
-
         function fecharModal() {
             document.getElementById('modalCliente').classList.remove('aberto');
         }
-
-        // Fecha o modal clicando fora
-        document.getElementById('modalCliente').addEventListener('click', function(e) {
+        document.getElementById('modalCliente').addEventListener('click', function (e) {
             if (e.target === this) fecharModal();
         });
+
+        function filtrarTabela() {
+            const termo = document.getElementById('campoBusca').value.toLowerCase();
+            const linhas = document.querySelectorAll('#tabelaClientes tbody tr');
+            linhas.forEach(linha => {
+                linha.style.display = linha.textContent.toLowerCase().includes(termo) ? '' : 'none';
+            });
+        }
     </script>
-}
\ No newline at end of file
+}
diff --git a/DBS/DBS/Views/Dashboard/Index.cshtml b/DBS/DBS/Views/Dashboard/Index.cshtml
index 8f6a334..3adca79 100644
--- a/DBS/DBS/Views/Dashboard/Index.cshtml
+++ b/DBS/DBS/Views/Dashboard/Index.cshtml
@@ -1,72 +1,73 @@
-﻿@{
+@model DBS.ViewModels.DashboardViewModel
+@{
     Layout = "~/Views/Shared/_Layout.cshtml";
 }
 
 @section Styles {
-        <link rel="stylesheet" href="~/css/dashboard.css" />
+    <link rel="stylesheet" href="~/css/dashboard.css" />
 }
 
-    <div class="dashboard-header">
-        <h1>Dashboard</h1>
-        <p>Visão geral do sistema</p>
-    </div>
+<div class="dashboard-header">
+    <h1>Dashboard</h1>
+    <p>Visão geral do sistema</p>
+</div>
 
-    <div class="cards-grid">
-        <div class="metric-card">
-            <span class="metric-label">Receita do Mês</span>
-            <span class="metric-value">R$ 48.320</span>
-            <span class="metric-delta positivo">↑ 12% em relação ao mês anterior</span>
-        </div>
-        <div class="metric-card">
-            <span class="metric-label">Clientes Ativos</span>
-            <span class="metric-value">1.240</span>
-            <span class="metric-delta positivo">↑ 8 novos hoje</span>
-        </div>
-        <div class="metric-card">
-            <span class="metric-label">Ordens Abertas</span>
-            <span class="metric-value">342</span>
-            <span class="metric-delta negativo">↓ 3 pendentes de ação</span>
-        </div>
-        <div class="metric-card">
-            <span class="metric-label">Produtos em Estoque</span>
-            <span class="metric-value">87</span>
-            <span class="metric-delta positivo">↑ Estoque saudável</span>
-        </div>
+<div class="cards-grid">
+    <div class="metric-card">
+        <span class="metric-label">Receita do Mês</span>
+        <span class="metric-value">R$ @Model.ReceitaMes.ToString("N2")</span>
+        <span class="metric-delta positivo">↑ Pedidos pagos no mês atual</span>
+    </div>
+    <div class="metric-card">
+        <span class="metric-label">Clientes Ativos</span>
+        <span class="metric-value">@Model.TotalClientesAtivos</span>
+        <span class="metric-delta positivo">↑ Total cadastrado</span>
+    </div>
+    <div class="metric-card">
+        <span class="metric-label">Ordens Abertas</span>
+        <span class="metric-value">@Model.TotalOrdensAbertas</span>
+        <span class="metric-delta negativo">↓ Pendentes de ação</span>
+    </div>
+    <div class="metric-card">
+        <span class="metric-label">Produtos em Estoque</span>
+        <span class="metric-value">@Model.TotalProdutosEstoque</span>
+        <span class="metric-delta positivo">↑ Unidades disponíveis</span>
     </div>
+</div>
 
-    <div class="dashboard-row">
-        <div class="painel">
-            <h3 class="painel-titulo">Últimas Ordens</h3>
-            <table class="tabela">
-                <thead>
-                    <tr>
-                        <th>Pedido</th>
-                        <th>Cliente</th>
-                        <th>Valor</th>
-                        <th>Status</th>
-                    </tr>
-                </thead>
-                <tbody>
-                    <tr>
-                        <td>#4521</td>
-                        <td>Maria Silva</td>
-                        <td>R$ 320,00</td>
-                        <td><span class="status pago">Pago</span></td>
-                    </tr>
+<div class="dashboard-row">
+    <div class="painel">
+        <h3 class="painel-titulo">Últimas Ordens</h3>
+        <table class="tabela">
+            <thead>
+                <tr>
+                    <th>Pedido</th>
+                    <th>Cliente</th>
+                    <th>Valor</th>
+                    <th>Status</th>
+                </tr>
+            </thead>
+            <tbody>
+                @if (!Model.UltimasOrdens.Any())
+                {
                     <tr>
-                        <td>#4520</td>
-                        <td>João Pereira</td>
-                        <td>R$ 150,00</td>
-                        <td><span class="status pendente">Pendente</span></td>
+                        <td colspan="4" style="text-align:center; color:#888;">Nenhuma ordem encontrada.</td>
                     </tr>
+                }
+                @foreach (var ordem in Model.UltimasOrdens)
+                {
                     <tr>
-                        <td>#4519</td>
-                        <td>Ana Costa</td>
-                        <td>R$ 890,00</td>
-                        <td><span class="status pago">Pago</span></td>
+                        <td>#@ordem.Id</td>
+                        <td>@ordem.ClienteNome</td>
+                        <td>R$ @ordem.ValorTotal.ToString("N2")</td>
+                        <td>
+                            <span class="status @(ordem.Status?.ToLower() == "pago" ? "pago" : "pendente")">
+                                @ordem.Status
+                            </span>
+                        </td>
                     </tr>
-                </tbody>
-            </table>
-        </div>
+                }
+            </tbody>
+        </table>
     </div>
-    
\ No newline at end of file
+</div>
diff --git a/DBS/DBS/Views/Ordens/Index.cshtml b/DBS/DBS/Views/Ordens/Index.cshtml
index f18d824..9eed2fd 100644
--- a/DBS/DBS/Views/Ordens/Index.cshtml
+++ b/DBS/DBS/Views/Ordens/Index.cshtml
@@ -1,5 +1,4 @@
-﻿@model List<DBS.Controllers.Ordem>
-
+@model List<DBS.Models.Pedido>
 @{
     ViewData["Title"] = "Ordens";
     ViewData["Pagina"] = "Ordens";
@@ -21,8 +20,8 @@
     </div>
 
     <div class="barra-busca">
-        <input type="text" placeholder="Buscar pedido..." />
-        <select>
+        <input type="text" id="campoBusca" placeholder="Buscar pedido..." oninput="filtrarTabela()" />
+        <select id="filtroStatus" onchange="filtrarTabela()">
             <option value="">Todos</option>
             <option value="pendente">Pendente</option>
             <option value="pago">Pago</option>
@@ -31,31 +30,43 @@
     </div>
 
     <div class="painel">
-        <table class="tabela">
+        <table class="tabela" id="tabelaOrdens">
             <thead>
                 <tr>
                     <th>Pedido</th>
                     <th>Cliente</th>
+                    <th>Data</th>
                     <th>Valor</th>
                     <th>Status</th>
                     <th>Ações</th>
                 </tr>
             </thead>
             <tbody>
+                @if (!Model.Any())
+                {
+                    <tr>
+                        <td colspan="6" style="text-align:center; color:#888;">Nenhuma ordem encontrada.</td>
+                    </tr>
+                }
                 @foreach (var ordem in Model)
                 {
                     <tr>
                         <td>#@ordem.Id</td>
                         <td>@ordem.ClienteNome</td>
-                        <td>R$ @ordem.Valor</td>
+                        <td>@(ordem.DataPedido != DateTime.MinValue ? ordem.DataPedido.ToString("dd/MM/yyyy") : "—")</td>
+                        <td>R$ @ordem.ValorTotal.ToString("N2")</td>
                         <td>
-                            <span class="status @(ordem.Status.ToLower())">
-                                @ordem.Status
+                            <span class="status @(ordem.Status?.ToLower() ?? "pendente")">
+                                @(ordem.Status ?? "Pendente")
                             </span>
                         </td>
                         <td>
                             <button class="btn-acao editar">Editar</button>
-                            <button class="btn-acao excluir">Excluir</button>
+                            <form method="post" asp-action="Excluir" asp-route-id="@ordem.Id" style="display:inline"
+                                  onsubmit="return confirm('Excluir ordem #@ordem.Id?')">
+                                @Html.AntiForgeryToken()
+                                <button type="submit" class="btn-acao excluir">Excluir</button>
+                            </form>
                         </td>
                     </tr>
                 }
@@ -78,11 +89,6 @@
             <input type="text" placeholder="Nome do cliente" />
         </div>
 
-        <div class="form-grupo">
-            <label>Valor</label>
-            <input type="number" placeholder="0,00" />
-        </div>
-
         <div class="form-grupo">
             <label>Status</label>
             <select>
@@ -104,13 +110,22 @@
         function abrirModal() {
             document.getElementById('modalOrdem').classList.add('aberto');
         }
-
         function fecharModal() {
             document.getElementById('modalOrdem').classList.remove('aberto');
         }
-
         document.getElementById('modalOrdem').addEventListener('click', function (e) {
             if (e.target === this) fecharModal();
         });
+
+        function filtrarTabela() {
+            const termo = document.getElementById('campoBusca').value.toLowerCase();
+            const status = document.getElementById('filtroStatus').value.toLowerCase();
+            const linhas = document.querySelectorAll('#tabelaOrdens tbody tr');
+            linhas.forEach(linha => {
+                const textoOk = linha.textContent.toLowerCase().includes(termo);
+                const statusOk = status === '' || linha.textContent.toLowerCase().includes(status);
+                linha.style.display = textoOk && statusOk ? '' : 'none';
+            });
+        }
     </script>
 }
diff --git a/DBS/DBS/Views/Produtos/Index.cshtml b/DBS/DBS/Views/Produtos/Index.cshtml
index 88d96cc..04126e1 100644
--- a/DBS/DBS/Views/Produtos/Index.cshtml
+++ b/DBS/DBS/Views/Produtos/Index.cshtml
@@ -1,4 +1,5 @@
-﻿@{
+@model List<DBS.Models.Produto>
+@{
     Layout = "~/Views/Shared/_Layout.cshtml";
     ViewData["Title"] = "Produtos";
     ViewData["Pagina"] = "Produtos";
@@ -20,24 +21,11 @@
     </div>
 
     <div class="barra-busca">
-        <input type="text" placeholder="Buscar por nome ou código..." />
-        <select>
-            <option value="">Todas as categorias</option>
-            <option value="eletronicos">Eletrônicos</option>
-            <option value="roupas">Roupas</option>
-            <option value="casa">Casa</option>
-            <option value="outros">Outros</option>
-        </select>
-        <select>
-            <option value="">Todos os status</option>
-            <option value="disponivel">Disponível</option>
-            <option value="indisponivel">Indisponível</option>
-            <option value="baixo-estoque">Baixo Estoque</option>
-        </select>
+        <input type="text" id="campoBusca" placeholder="Buscar por nome ou código..." oninput="filtrarTabela()" />
     </div>
 
     <div class="painel">
-        <table class="tabela">
+        <table class="tabela" id="tabelaProdutos">
             <thead>
                 <tr>
                     <th>Código</th>
@@ -50,66 +38,31 @@
                 </tr>
             </thead>
             <tbody>
-                <tr>
-                    <td>PRD-001</td>
-                    <td>Notebook Dell Inspiron</td>
-                    <td>Eletrônicos</td>
-                    <td>R$ 3.200,00</td>
-                    <td>12</td>
-                    <td><span class="status disponivel">Disponível</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
-                <tr>
-                    <td>PRD-002</td>
-                    <td>Mouse Logitech MX</td>
-                    <td>Eletrônicos</td>
-                    <td>R$ 280,00</td>
-                    <td class="estoque-baixo">3</td>
-                    <td><span class="status baixo-estoque">Baixo Estoque</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
-                <tr>
-                    <td>PRD-003</td>
-                    <td>Camiseta Polo</td>
-                    <td>Roupas</td>
-                    <td>R$ 89,90</td>
-                    <td>45</td>
-                    <td><span class="status disponivel">Disponível</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
-                <tr>
-                    <td>PRD-004</td>
-                    <td>Cadeira Ergonômica</td>
-                    <td>Casa</td>
-                    <td>R$ 1.450,00</td>
-                    <td>0</td>
-                    <td><span class="status indisponivel">Indisponível</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
-                <tr>
-                    <td>PRD-005</td>
-                    <td>Monitor Samsung 24"</td>
-                    <td>Eletrônicos</td>
-                    <td>R$ 1.100,00</td>
-                    <td>8</td>
-                    <td><span class="status disponivel">Disponível</span></td>
-                    <td>
-                        <button class="btn-acao editar">Editar</button>
-                        <button class="btn-acao excluir">Excluir</button>
-                    </td>
-                </tr>
+                @if (!Model.Any())
+                {
+                    <tr>
+                        <td colspan="7" style="text-align:center; color:#888;">Nenhum produto cadastrado.</td>
+                    </tr>
+                }
+                @foreach (var produto in Model)
+                {
+                    <tr>
+                        <td>PRD-@produto.Id.ToString("D3")</td>
+                        <td>@produto.Nome</td>
+                        <td>@(produto.CategoriaNome ?? "—")</td>
+                        <td>R$ @produto.Preco.ToString("N2")</td>
+                        <td class="@(produto.Estoque <= 5 && produto.Estoque > 0 ? "estoque-baixo" : "")">@produto.Estoque</td>
+                        <td><span class="status @produto.StatusEstoque">@produto.StatusEstoqueLabel</span></td>
+                        <td>
+                            <button class="btn-acao editar">Editar</button>
+                            <form method="post" asp-action="Excluir" asp-route-id="@produto.Id" style="display:inline"
+                                  onsubmit="return confirm('Excluir produto @produto.Nome?')">
+                                @Html.AntiForgeryToken()
+                                <button type="submit" class="btn-acao excluir">Excluir</button>
+                            </form>
+                        </td>
+                    </tr>
+                }
             </tbody>
         </table>
     </div>
@@ -124,23 +77,6 @@
             <button class="modal-fechar" onclick="fecharModal()">×</button>
         </div>
 
-        <div class="form-linha">
-            <div class="form-grupo">
-                <label>Código</label>
-                <input type="text" placeholder="Ex: PRD-006" />
-            </div>
-            <div class="form-grupo">
-                <label>Categoria</label>
-                <select>
-                    <option value="">Selecione...</option>
-                    <option value="eletronicos">Eletrônicos</option>
-                    <option value="roupas">Roupas</option>
-                    <option value="casa">Casa</option>
-                    <option value="outros">Outros</option>
-                </select>
-            </div>
-        </div>
-
         <div class="form-grupo">
             <label>Nome do Produto</label>
             <input type="text" placeholder="Ex: Notebook Dell Inspiron" />
@@ -162,14 +98,6 @@
             </div>
         </div>
 
-        <div class="form-grupo">
-            <label>Status</label>
-            <select>
-                <option value="disponivel">Disponível</option>
-                <option value="indisponivel">Indisponível</option>
-            </select>
-        </div>
-
         <div class="modal-footer">
             <button class="btn-cancelar" onclick="fecharModal()">Cancelar</button>
             <button class="btn-salvar">Salvar Produto</button>
@@ -182,13 +110,19 @@
         function abrirModal() {
             document.getElementById('modalProduto').classList.add('aberto');
         }
-
         function fecharModal() {
             document.getElementById('modalProduto').classList.remove('aberto');
         }
-
-        document.getElementById('modalProduto').addEventListener('click', function(e) {
+        document.getElementById('modalProduto').addEventListener('click', function (e) {
             if (e.target === this) fecharModal();
         });
+
+        function filtrarTabela() {
+            const termo = document.getElementById('campoBusca').value.toLowerCase();
+            const linhas = document.querySelectorAll('#tabelaProdutos tbody tr');
+            linhas.forEach(linha => {
+                linha.style.display = linha.textContent.toLowerCase().includes(termo) ? '' : 'none';
+            });
+        }
     </script>
-}
\ No newline at end of file
+}
diff --git a/DBS/DBS/Views/Vendas/Index.cshtml b/DBS/DBS/Views/Vendas/Index.cshtml
index b594b9f..f533cfd 100644
--- a/DBS/DBS/Views/Vendas/Index.cshtml
+++ b/DBS/DBS/Views/Vendas/Index.cshtml
@@ -1,5 +1,4 @@
-﻿@model List<DBS.Controllers.Venda>
-
+@model List<DBS.Models.Pedido>
 @{
     ViewData["Title"] = "Vendas";
     ViewData["Pagina"] = "Vendas";
@@ -21,8 +20,8 @@
     </div>
 
     <div class="barra-busca">
-        <input type="text" placeholder="Buscar cliente..." />
-        <select>
+        <input type="text" id="campoBusca" placeholder="Buscar cliente..." oninput="filtrarTabela()" />
+        <select id="filtroStatus" onchange="filtrarTabela()">
             <option value="">Todos</option>
             <option value="pago">Pago</option>
             <option value="pendente">Pendente</option>
@@ -30,7 +29,7 @@
     </div>
 
     <div class="painel">
-        <table class="tabela">
+        <table class="tabela" id="tabelaVendas">
             <thead>
                 <tr>
                     <th>ID</th>
@@ -42,16 +41,22 @@
                 </tr>
             </thead>
             <tbody>
+                @if (!Model.Any())
+                {
+                    <tr>
+                        <td colspan="6" style="text-align:center; color:#888;">Nenhuma venda registrada.</td>
+                    </tr>
+                }
                 @foreach (var venda in Model)
                 {
                     <tr>
                         <td>#@venda.Id</td>
                         <td>@venda.ClienteNome</td>
-                        <td>R$ @venda.Valor</td>
-                        <td>@venda.Data.ToString("dd/MM/yyyy")</td>
+                        <td>R$ @venda.ValorTotal.ToString("N2")</td>
+                        <td>@(venda.DataPedido != DateTime.MinValue ? venda.DataPedido.ToString("dd/MM/yyyy") : "—")</td>
                         <td>
-                            <span class="status @(venda.Status == "Pago" ? "pago" : "pendente")">
-                                @venda.Status
+                            <span class="status @(venda.Status?.ToLower() == "pago" ? "pago" : "pendente")">
+                                @(venda.Status ?? "Pendente")
                             </span>
                         </td>
                         <td>
@@ -66,7 +71,7 @@
 
 </div>
 
-<!-- Modal de Cadastro -->
+<!-- Modal -->
 <div class="modal-overlay" id="modalVenda">
     <div class="modal">
         <div class="modal-header">
@@ -79,11 +84,6 @@
             <input type="text" placeholder="Nome do cliente" />
         </div>
 
-        <div class="form-grupo">
-            <label>Valor</label>
-            <input type="number" placeholder="0,00" />
-        </div>
-
         <div class="form-grupo">
             <label>Status</label>
             <select>
@@ -104,13 +104,22 @@
         function abrirModal() {
             document.getElementById('modalVenda').classList.add('aberto');
         }
-
         function fecharModal() {
             document.getElementById('modalVenda').classList.remove('aberto');
         }
-
         document.getElementById('modalVenda').addEventListener('click', function (e) {
             if (e.target === this) fecharModal();
         });
+
+        function filtrarTabela() {
+            const termo = document.getElementById('campoBusca').value.toLowerCase();
+            const status = document.getElementById('filtroStatus').value.toLowerCase();
+            const linhas = document.querySelectorAll('#tabelaVendas tbody tr');
+            linhas.forEach(linha => {
+                const textoOk = linha.textContent.toLowerCase().includes(termo);
+                const statusOk = status === '' || linha.textContent.toLowerCase().includes(status);
+                linha.style.display = textoOk && statusOk ? '' : 'none';
+            });
+        }
     </script>
 }
diff --git a/DBS/DBS/appsettings.json b/DBS/DBS/appsettings.json
index 10f68b8..69e9d19 100644
--- a/DBS/DBS/appsettings.json
+++ b/DBS/DBS/appsettings.json
@@ -5,5 +5,8 @@
       "Microsoft.AspNetCore": "Warning"
     }
   },
-  "AllowedHosts": "*"
+  "AllowedHosts": "*",
+  "ConnectionStrings": {
+    "DefaultConnection": "Server=localhost;Port=3306;Database=techpoint informatica;User=root;Password=SUA_SENHA_AQUI;"
+  }
 }
-- 
2.43.0

